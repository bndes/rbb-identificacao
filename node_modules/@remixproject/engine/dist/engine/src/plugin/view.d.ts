import { Plugin } from './abstract';
import { Profile, LocationProfile } from '../../../utils';
export declare function isView<P extends Profile>(profile: Profile): profile is (ViewProfile & P);
export declare type ViewProfile = Profile & LocationProfile;
export declare abstract class ViewPlugin extends Plugin {
    profile: ViewProfile;
    abstract render(): Element;
    constructor(profile: ViewProfile);
    activate(): Promise<void>;
    deactivate(): void;
}
