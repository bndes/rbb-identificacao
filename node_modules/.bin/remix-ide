#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../remix-ide/bin/remix-ide" "$@"
  ret=$?
else 
  node  "$basedir/../remix-ide/bin/remix-ide" "$@"
  ret=$?
fi
exit $ret
